- name: Ensure grafana-dashboards directory exists
  ansible.builtin.file:
    path: /srv/data/monitoring/grafana-dashboards
    state: directory
    owner: 472
    group: 472
    mode: "0755"

- name: Copy Grafana dashboards
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: /srv/data/monitoring/grafana-dashboards/
    owner: 472
    group: 472
    mode: "0644"
  loop:
    - "{{ role_path }}/files/homelab-dashboard.json"
    - "{{ role_path }}/files/homelab-human-dashboard.json"

- name: Apply Prometheus alert rules
  kubernetes.core.k8s:
    state: present
    definition: "{{ lookup('file', playbook_dir + '/../k8s/monitoring/alerts.yaml') | from_yaml_all | list }}"
    kubeconfig: /etc/rancher/k3s/k3s.yaml

- name: Apply Alertmanager
  kubernetes.core.k8s:
    state: present
    template: "{{ playbook_dir }}/../k8s/monitoring/alertmanager.yaml.j2"
    kubeconfig: /etc/rancher/k3s/k3s.yaml

- name: Apply node-exporter DaemonSet
  kubernetes.core.k8s:
    state: present
    definition: "{{ lookup('file', playbook_dir + '/../k8s/monitoring/node-exporter.yaml') | from_yaml_all | list }}"
    kubeconfig: /etc/rancher/k3s/k3s.yaml

- name: Apply cadvisor DaemonSet
  kubernetes.core.k8s:
    state: present
    definition: "{{ lookup('file', playbook_dir + '/../k8s/monitoring/cadvisor.yaml') | from_yaml_all | list }}"
    kubeconfig: /etc/rancher/k3s/k3s.yaml

- name: Apply Prometheus
  kubernetes.core.k8s:
    state: present
    definition: "{{ lookup('file', playbook_dir + '/../k8s/monitoring/prometheus.yaml') | from_yaml_all | list }}"
    kubeconfig: /etc/rancher/k3s/k3s.yaml

- name: Apply Loki
  kubernetes.core.k8s:
    state: present
    definition: "{{ lookup('file', playbook_dir + '/../k8s/monitoring/loki.yaml') | from_yaml_all | list }}"
    kubeconfig: /etc/rancher/k3s/k3s.yaml

- name: Apply Promtail DaemonSet
  kubernetes.core.k8s:
    state: present
    definition: "{{ lookup('file', playbook_dir + '/../k8s/monitoring/promtail.yaml') | from_yaml_all | list }}"
    kubeconfig: /etc/rancher/k3s/k3s.yaml

- name: Apply Grafana
  kubernetes.core.k8s:
    state: present
    definition: "{{ lookup('file', playbook_dir + '/../k8s/monitoring/grafana.yaml') | from_yaml_all | list }}"
    kubeconfig: /etc/rancher/k3s/k3s.yaml
