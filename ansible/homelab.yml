- name: Homelab
  hosts: localhost
  connection: local
  # Makes ansible run as root
  become: true
  roles:
    - system
    - storage
    - docker
    - k3s
    - k3s_secrets
    - monitoring_k8s
    - traefik_k8s
    - immich
    - nextcloud
    - minecraft_k8s
    - n8n_k8s
    - backup